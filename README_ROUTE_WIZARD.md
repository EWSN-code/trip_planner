# Route Wizard update

## Added
- 旅程追加メニュー
- 移動一括追加ウィザード
- 複数区間と乗換の自動生成
- 直前到着地点の引継ぎ
- station / transport / transferへの変換
- 項目複製
- 同一日内の上下移動
- 個別追加時の費用科目初期値

既存のSupabase設定 `config.js` は含めていません。現行リポジトリの設定済み `config.js` をそのまま残してください。
