# v2.2.2b hotfix

原因は、前回ZIPの `index.html` に新しいボタンとダイアログが挿入されていない一方、`app.js` がそれらを参照し、初期描画中に停止していたことです。

## 修正
- `旅程を編集` ボタンを確実に追加
- 初期状態を表示モードに固定
- 右下 `＋` と末尾追加ボタンを確実に追加・接続
- `[hidden]` のCSSを明示
- 費用実績入力ダイアログを追加
- 別駅乗換の入力欄を追加

`config.js` と `print/` は現行のまま残してください。
