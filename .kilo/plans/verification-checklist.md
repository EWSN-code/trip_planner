# Step 6: 旅行比較機能 検証チェックリスト

実装サマリ:
- `}` 修正: `js/app.js` の `actualBlock` 関数 template literal 末尾の不要 `}` 削除
- 比較画面を閉じる: `js/app.js` の `compareBackBtn` ハンドラで `#compareView` を非表示 + 内部クリア + 状態リセット
- 片方のみ表示: `js/render.js` の `renderCompareDay` を `.compare-side[data-side="a|b"]` 構造に変更、`js/app.js` の `showCompare` に A/B 切替タブ UI を追加
- CSS: `styles.css` に `.compare-tabs` / `.compare-tab` / `.compare-grid` / `.compare-day` / `.compare-side` / `.compare-item` / レスポンシブ(650px)を追加

---

## 1. `}` が出力されないこと

| # | 事前条件 | 操作 | 期待 |
|---|---------|------|------|
| 1-1 | 旅程詳細画面にいる | 「実績を入力」ボタン (交通費など実績未入力の項目の下) を確認 | ボタンのテキストが「実績を入力」、直後に `}` 文字が表示されない |
| 1-2 | DevTools で当該 `<button>` を選択 | `outerHTML` を確認 | `<button>実績を入力</button></div>` で閉じ、`}` 文字が DOM に存在しない |
| 1-3 | 複数項目の旅程 (観光・食事・宿泊・移動) | それぞれの下で「実績を入力」/「実績を修正」ボタンを確認 | `}` 文字が表示されない |

## 2. 比較画面を閉じると比較 UI が消える

| # | 事前条件 | 操作 | 期待 |
|---|---------|------|------|
| 2-1 | 旅行が 2 件以上ある | 比較モード ON → 2 件選択 → 「比較」クリック | 比較画面 (`#compareView`) が表示される |
| 2-2 | 比較画面表示中 | DevTools で `#compareView` を確認 | `hidden` 属性が付いていない |
| 2-3 | 比較画面表示中 | 「← 旅行一覧」ボタン (比較画面ヘッダの戻る) をクリック | 旅行一覧 (`#listView`) に戻る |
| 2-4 | 2-3 直後 | DevTools で `#compareView` を確認 | `hidden` 属性が付いている (`display: none` 相当) |
| 2-5 | 2-3 直後 | DevTools で `#compareView .innerHTML` を確認 | 空文字 (クリア済み) |
| 2-6 | 2-3 直後 | 「比較モード」トグルを確認 | OFF (チェックなし) に戻っている |
| 2-7 | 2-3 直後 | 一覧の各カードを確認 | チェックボックス / ラジオが表示されていない (トグル OFF 状態) |

## 3. 選択した旅行の旅程のみ表示 + 切替 UI

| # | 事前条件 | 操作 | 期待 |
|---|---------|------|------|
| 3-1 | 旅行 A「京都」(2 日分)、旅行 B「大阪」(3 日分) を作成 | 比較モード ON → 2 件選択 → 比較 | `#compareDays` 内に各日 (1日目, 2日目, 3日目) の `.compare-day` セクションが表示される |
| 3-2 | 3-1 直後 | デフォルト表示 (A タブ) | 各日の旅行 A の旅程のみ表示、B 側 (`.compare-side[data-side="b"]`) は `hidden` |
| 3-3 | 3-2 直後 | `#compareTabs` を確認 | 「A: 京都」「B: 大阪」ボタンがあり、A 側が `active` |
| 3-4 | 3-2 直後 | B タブをクリック | 旅行 B の旅程に切り替わり、A 側が `hidden`、B タブが `active` |
| 3-5 | 3-4 直後 | A タブを再度クリック | A の旅程に戻る |
| 3-6 | 3-2 直後 | 1 日目に旅行 B のみ存在する (例: B に 3日目のみあり) | 1 日目の B 側を切り替えると B の項目が表示、A 側はその日に項目がなければ「予定なし」 |
| 3-7 | 比較画面表示中 | DevTools で `#compareDays .compare-side` を確認 | A 側要素は表示、B 側要素は `hidden` 属性 (切替後は逆) |
| 3-8 | 比較画面表示中 | `#floatingAddBtn` (右下の＋) を確認 | `hidden` 属性が付いていて非表示 (編集不可を視覚化) |

## 4. 既存機能リグレッション

| # | 事前条件 | 操作 | 期待 |
|---|---------|------|------|
| 4-1 | 旅行一覧にいる | 比較モード OFF | トグル / 比較ボタンは表示されるが、各カードにチェックボックスは出ない |
| 4-2 | 旅行一覧にいる | 比較モード ON で 0 件選択 | 「比較」ボタンが disabled |
| 4-3 | 旅行一覧にいる | 比較モード ON で 1 件選択 | 「比較」ボタンが disabled |
| 4-4 | 旅行一覧にいる | 比較モード ON で 2 件選択 | 「比較」ボタンが enabled |
| 4-5 | 旅行一覧にいる | 比較モード ON で 3 件目をクリック | 3 件目は選択されない (2 件制限) |
| 4-6 | 旅程詳細 (`#detailView`) を開く | 通常表示 | 旅程が正しく表示される |
| 4-7 | 旅程詳細 | 編集モードに切替 | 各項目に編集/削除/複製ボタンが現れる |
| 4-8 | 旅程詳細 | 「← 旅行一覧」で戻る | 一覧に戻る。比較モード状態は影響しない |
| 4-9 | ログアウト (`#logoutBtn`) | 比較選択状態が消える (揮発性) | `Storage.save` が比較状態を保存していないので、再ログイン後も比較選択は空 |
| 4-10 | 印刷 (Ctrl+P) | 比較画面の印刷プレビュー | 1〜2 ページに収まる (A/B タブは印刷非表示でよい) |
| 4-11 | 印刷 URL `print/index.html?trip=ID` | 該当旅行を印刷 | 既存どおり旅程 1 件のみが表示される |

## 5. レスポンシブ

| # | 画面幅 | 操作 | 期待 |
|---|--------|------|------|
| 5-1 | 1024px 以上 | 比較画面 | サマリが 2 列、`.compare-day` も 2 列想定だが今は 1 列表示 (片方のみ) なので問題なし |
| 5-2 | 650px 以下 | 比較画面 | サマリ `.compare-grid` が 1 列に折り返す |
| 5-3 | 650px 以下 | 比較画面 | タブボタンが折り返し表示で読みやすい |
| 5-4 | 650px 以下 | 旅程詳細 | 既存のリグレッションなし (`.top-summary` 1 列など) |

## 6. Supabase 同期

| # | 事前条件 | 操作 | 期待 |
|---|---------|------|------|
| 6-1 | ログイン済み | 比較画面で旅行 A を選択 | Supabase に比較状態が保存されない (Network タブで `state` PUT が比較選択を含まない) |
| 6-2 | ログイン済み | 別デバイスで同期 | 比較選択状態は同期されない (揮発性のため) |

## 7. エラーケース

| # | シナリオ | 期待 |
|---|---------|------|
| 7-1 | 旅行 0 件で比較モード ON | トグルは出るが 0 件なので「比較」ボタンは disabled のまま |
| 7-2 | 旅行 1 件で比較モード ON | 1 件選択しても「比較」ボタンは disabled |
| 7-3 | 旅行 2 件で 2 件選択し、片方を別操作で削除 | 比較画面で `#listView` に戻ると、削除された旅行の選択は無効化 |
| 7-4 | 比較画面表示中に `#tripDialog` (旅行編集) を開閉 | 比較画面は影響を受けない |

---

## 完了基準

- [ ] セクション 1〜7 のすべての項目が期待動作
- [ ] 既存の旅程詳細 / 編集モード / 印刷 / Supabase 同期にリグレッションなし
- [ ] DevTools コンソールに JS エラーが出ていない
- [ ] 650px 以下でレイアウト崩れなし

## 既知の制約

- 比較選択は揮発性 (リロード / ログアウトで消える) — 設計通り
- A/B 切替はクライアント側のみ、URL に状態を持たない
- 印刷時は A/B 切替 UI は出ない (`.compare-tabs` は印刷スタイルで非表示にしない場合そのまま出る)
